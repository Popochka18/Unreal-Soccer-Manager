---
description: Design or extend the modding surface.
argument-hint: <topic: pack layering | rules DSL | theming | facepacks | strings>
allowed-tools: Read, Write, Edit, Bash
---
Topic: $ARGUMENTS

Test to satisfy: a modder with a text editor and an image folder can do this without
compiling anything and without reading our source.

Deliver: the data format (json5/csv), its JSON-Schema, compiler ingestion, conflict-
resolution behaviour across pack layers, human-readable validation errors with file:line,
a worked example mod under /data/packs/example-mod/, and a docs page in /docs/modding/.

Explicitly list what is still NOT moddable and why.
