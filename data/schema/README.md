# Data pack JSON-Schema

Empty until M1. One schema per data-pack file. Validation is CI-gated.

These schemas are the modding contract: they are what lets someone with a text editor build a
football world without reading our source. Validation errors must name the file and line and
say what was expected — a modder cannot debug `assertion failed`.
