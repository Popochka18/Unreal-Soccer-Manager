# §4: season/world simulation — calendar, AI managers, finance, transfers,
# player development.
#
# Placeholder. Lands at M3 (§12); do not add targets here ahead of that without
# an ADR. The directory exists so that the canonical layout in §4 is real on
# disk and so `git log` shows when this subsystem actually started.
