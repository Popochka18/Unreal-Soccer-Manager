# §4: headless server binary — HTTP + WebSocket, save/load, DB access, job
# scheduler. No UI, no engine dependencies.
#
# Placeholder. Lands at M3 (§12). The IPC surface it exposes is generated from
# /docs/contracts/ipc.schema.json; see the /api command before adding anything.
