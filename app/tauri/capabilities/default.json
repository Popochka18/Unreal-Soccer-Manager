{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline window capability. Deliberately minimal: this shell is a launcher and a bridge, so it is granted nothing it does not demonstrably need.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
